# 릴리즈 — main 에서 수동 dispatch 시 사이드카 dist 아카이브를 빌드해 owner-immutable
# GitHub Release 자산으로 올린다. 버전/태그는 소유 manifest(Cargo.toml)에서 유도한다.
# 소비측(플러그인 manifest sidecars[].reach.fetch = 이 자산 URL + sha256 핀)은 코어에 구현·검증 완료.
name: release

on:
  workflow_dispatch:

concurrency:
  group: release-${{ github.repository }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  release-darwin-arm64:
    if: github.ref == 'refs/heads/main'
    runs-on: macos-14
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      - uses: dtolnay/rust-toolchain@4be7066ada62dd38de10e7b70166bc74ed198c30
        with:
          toolchain: "1.96.0"
      - id: identity
        run: |
          ver="$(sed -n 's/^version = "\(.*\)"/\1/p' Cargo.toml | head -1)"
          case "$ver" in ''|*[!0-9.]*) echo "invalid version: $ver" >&2; exit 1;; esac
          echo "version=$ver" >> "$GITHUB_OUTPUT"
          echo "tag=v$ver" >> "$GITHUB_OUTPUT"
      # The engine (libghostty-vt.a) is a Zig static library built from pinned ghostty
      # source, not a cargo dependency; build.rs links it. Zig 0.15.2 is ghostty's
      # minimum_zig_version and the commit is pinned on purpose (README build requirements).
      - name: Build libghostty-vt from pinned ghostty source (Zig)
        run: |
          set -euo pipefail
          curl -fsSL https://ziglang.org/download/0.15.2/zig-aarch64-macos-0.15.2.tar.xz -o zig.tar.xz
          mkdir -p _zig && tar -xJf zig.tar.xz -C _zig --strip-components=1
          zig="$PWD/_zig/zig"
          "$zig" version
          git init -q _ghostty
          git -C _ghostty remote add origin https://github.com/ghostty-org/ghostty.git
          git -C _ghostty fetch -q --depth 1 origin a887df42c56f6de86c0fe6da9c4eeca37931e083
          git -C _ghostty -c advice.detachedHead=false checkout -q a887df42c56f6de86c0fe6da9c4eeca37931e083
          ( cd _ghostty && "$zig" build -Demit-lib-vt=true -Doptimize=ReleaseFast )
          test -f _ghostty/zig-out/lib/libghostty-vt.a
          echo "SOKSAK_GHOSTTY_VT_LIB=$PWD/_ghostty/zig-out/lib" >> "$GITHUB_ENV"
      - run: cargo build --release --bin soksak-sidecar-terminal-ghostty
      - run: ./stage.sh /tmp/dist
      - id: archive
        run: |
          ver="${{ steps.identity.outputs.version }}"
          out="soksak-sidecar-terminal-ghostty-$ver-darwin-arm64.tar.gz"
          /usr/bin/tar -czLf "$out" -C /tmp/dist .
          shasum -a 256 "$out" | tee "$out.sha256"
          echo "asset=$out" >> "$GITHUB_OUTPUT"
      - name: Create least-privilege release token
        id: release-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1
        with:
          client-id: ${{ vars.SOKSAK_RELEASE_CLIENT_ID }}
          private-key: ${{ secrets.SOKSAK_RELEASE_PRIVATE_KEY }}
          permission-administration: read
          permission-contents: write
      - name: Publish through owner-enforced immutable releases
        env:
          GH_TOKEN: ${{ steps.release-token.outputs.token }}
        run: |
          enforced="$(gh api "repos/${{ github.repository }}/immutable-releases" --jq '.enabled and .enforced_by_owner')"
          test "$enforced" = "true" || { echo "owner-enforced immutable releases must be enabled before tagging" >&2; exit 1; }
          tag="${{ steps.identity.outputs.tag }}"
          gh release create "$tag" --repo "${{ github.repository }}" --target "${{ github.sha }}" --title "$tag" --generate-notes \
            "${{ steps.archive.outputs.asset }}" "${{ steps.archive.outputs.asset }}.sha256"
